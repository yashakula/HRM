# Production Environment Configuration
# This file contains environment variables for production deployment
# IMPORTANT: Update these values for your production environment

# Database Configuration (Use secure passwords in production)
POSTGRES_DB=hrms
POSTGRES_USER=postgres
POSTGRES_PASSWORD=mysecretpassword
DATABASE_URL=postgresql://postgres:mysecretpassword@database:5432/hrms

# API URLs for Production
# For testing production mode locally, use internal Docker network
NEXT_PUBLIC_API_URL=http://backend:8000
# For actual production deployment, update with your domain:
# NEXT_PUBLIC_API_URL=https://yourdomain.com

# Backend CORS Configuration (Update with your domain)
BACKEND_CORS_ORIGINS=https://yourdomain.com

# Application Settings
NODE_ENV=production
CREATE_SEED_DATA=false

# Production Security
DEBUG=false
VERBOSE_LOGGING=false

# Additional Production Variables (uncomment and configure as needed)
# SSL_CERT_PATH=/path/to/ssl/cert.pem
# SSL_KEY_PATH=/path/to/ssl/private.key
# BACKUP_SCHEDULE=0 2 * * *  # Daily backup at 2 AM